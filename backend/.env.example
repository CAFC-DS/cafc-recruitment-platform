# Railway Production Environment Variables
# Copy these to Railway dashboard under Variables tab
# CRITICAL: These must be set in Railway for CORS to work correctly

# Database Configuration (Snowflake)
# The application uses different Snowflake accounts based on ENVIRONMENT variable

# ===== DEVELOPMENT CONFIGURATION (Local Testing) =====
# Used when ENVIRONMENT=development
# Uses your personal Snowflake account with development warehouse
SNOWFLAKE_DEV_ACCOUNT=your_account.region.snowflakecomputing.com
SNOWFLAKE_DEV_USERNAME=YOUR_PERSONAL_USERNAME
SNOWFLAKE_DEV_ROLE=DEV_ROLE
SNOWFLAKE_DEV_WAREHOUSE=DEVELOPMENT_WH
SNOWFLAKE_DEV_DATABASE=RECRUITMENT_TEST
SNOWFLAKE_DEV_SCHEMA=PUBLIC
SNOWFLAKE_DEV_PRIVATE_KEY_PATH=./keys/rsa_key_unencrypted.pem

# ===== PRODUCTION CONFIGURATION (Deployed App) =====
# Used when ENVIRONMENT=production
# Uses dedicated APP_USER account with production warehouse
SNOWFLAKE_PROD_ACCOUNT=your_account.region.snowflakecomputing.com
SNOWFLAKE_PROD_USERNAME=APP_USER
SNOWFLAKE_PROD_ROLE=APP_ROLE
SNOWFLAKE_PROD_WAREHOUSE=COMPUTE_WH
SNOWFLAKE_PROD_DATABASE=RECRUITMENT_TEST
SNOWFLAKE_PROD_SCHEMA=PUBLIC
SNOWFLAKE_PROD_PRIVATE_KEY_PATH=./keys/app_user_rsa_key.pem
# For Railway deployment, use SNOWFLAKE_PRIVATE_KEY with the PEM file contents as a string

# ===== LEGACY CONFIGURATION (Backward Compatibility) =====
# These variables are fallbacks if DEV/PROD variables are not set
SNOWFLAKE_USERNAME=your_snowflake_username
SNOWFLAKE_PASSWORD=your_snowflake_password
SNOWFLAKE_ACCOUNT=your_account.region.snowflakecomputing.com
SNOWFLAKE_WAREHOUSE=your_warehouse_name
SNOWFLAKE_DATABASE=your_database_name
SNOWFLAKE_SCHEMA=PUBLIC
SNOWFLAKE_PRIVATE_KEY_PATH=./keys/rsa_key_unencrypted.pem

# Security Configuration
SECRET_KEY=your-super-secret-jwt-key-at-least-32-characters-long
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CRITICAL: CORS Configuration - MUST BE SET FOR LOGIN TO WORK
# Set ENVIRONMENT=production in Railway Variables tab
ENVIRONMENT=production
# Set CORS_ORIGINS to include your Vercel URL
CORS_ORIGINS=https://cafc-recruitment-platform.vercel.app,https://your-custom-domain.com
DEBUG=False

# DEPLOYMENT INSTRUCTIONS:
# 1. Generate RSA key-pair for APP_USER:
#    cd backend && python generate_app_keypair.py
# 2. Register the public key with Snowflake APP_USER (see script output)
# 3. Go to Railway dashboard > Your project > Variables tab
# 4. Add the PRODUCTION environment variables (SNOWFLAKE_PROD_*)
# 5. Add SNOWFLAKE_PRIVATE_KEY with contents of app_user_rsa_key.pem
# 6. Set ENVIRONMENT=production
# 7. Set CORS_ORIGINS to include your Vercel URL
# 8. Redeploy the service after adding variables
# 9. Check logs for connection info to verify correct account/warehouse is used

# LOCAL DEVELOPMENT INSTRUCTIONS:
# 1. Copy this file to .env
# 2. Fill in your personal Snowflake account details in SNOWFLAKE_DEV_* variables
# 3. Set ENVIRONMENT=development
# 4. Your personal key should be at ./keys/rsa_key_unencrypted.pem
# 5. Run: python main.py
# 6. The app will use DEVELOPMENT_WH and your personal account

# Email Configuration (for feedback system)
# Required for feedback notifications to work
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USERNAME=your_email@gmail.com
EMAIL_PASSWORD=your_gmail_app_password
ADMIN_EMAIL=admin@domain.com