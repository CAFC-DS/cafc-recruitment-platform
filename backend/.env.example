# Railway Production Environment Variables
# Copy these to Railway dashboard under Variables tab
# CRITICAL: These must be set in Railway for CORS to work correctly

# Database Configuration (Snowflake)
SNOWFLAKE_USERNAME=your_snowflake_username
SNOWFLAKE_PASSWORD=your_snowflake_password
SNOWFLAKE_ACCOUNT=your_account.region.snowflakecomputing.com
SNOWFLAKE_WAREHOUSE=your_warehouse_name
SNOWFLAKE_DATABASE=your_database_name
SNOWFLAKE_SCHEMA=PUBLIC

# Security Configuration
SECRET_KEY=your-super-secret-jwt-key-at-least-32-characters-long
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CRITICAL: CORS Configuration - MUST BE SET FOR LOGIN TO WORK
# Set ENVIRONMENT=production in Railway Variables tab
ENVIRONMENT=production
# Set CORS_ORIGINS to include your Vercel URL
CORS_ORIGINS=https://cafc-recruitment-platform.vercel.app,https://your-custom-domain.com
DEBUG=False

# DEPLOYMENT INSTRUCTIONS:
# 1. Go to Railway dashboard > Your project > Variables tab
# 2. Add ALL the above environment variables
# 3. Pay special attention to ENVIRONMENT=production and CORS_ORIGINS
# 4. Redeploy the service after adding variables
# 5. Check logs for "CORS Configuration Debug" output to verify

# Optional: For email features (future)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USERNAME=your_email@domain.com
# EMAIL_PASSWORD=your_app_password